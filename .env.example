# ローカル開発: このファイルを .dev.vars にコピー
# 本番 secrets: npm run secrets:generate → npm run secrets:production

SESSION_SECRET=change-me-local-only
DEMO_PASSWORD=changeme
SITE_URL=http://localhost:8788
CUSTOMER_PORTAL_SECRET=dev-portal-secret-change-me
QUOTE_SHARE_SECRET=dev-quote-secret-change-me
OPT_OUT_SECRET=dev-optout-secret-change-me

# LINE（本番は .production-secrets.json に追記 → npm run secrets:line）
# LINE_CHANNEL_ACCESS_TOKEN=
# LINE_CHANNEL_SECRET=
